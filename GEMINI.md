# GEMINI.md — Antigravity 个人记忆系统集成指令

## 记忆系统

本项目集成了用户的个人AI记忆系统，存放在 `~/MEMORY/`。为避免云盘同步时符号链接断开，**项目中请直接使用绝对路径 `~/MEMORY/` 访问**，不再使用符号链接。

**在每次对话开始时**，如果对话涉及需要了解用户的场景，请主动读取以下记忆文件（按需读取，不必全部读取）：

| 优先级 | 文件路径 | 内容 |
|:---:|---|---|
| 高 | `~/MEMORY/L2_情境上下文/当前关注.md` | 用户近期在关注什么 |
| 高 | `~/MEMORY/L3_行为模式/沟通偏好.md` | 用户希望的沟通方式 |
| 中 | `~/MEMORY/L2_情境上下文/近期上下文.md` | 用户近期背景信息 |
| 中 | `~/MEMORY/L3_行为模式/决策风格.md` | 用户做决策的方式 |
| 中 | `~/MEMORY/GOALS/短期目标.md` | 用户当前的目标 |
| 中 | `~/MEMORY/GOALS/决策红线.md` | 用户的底线和约束 |
| 低 | `~/MEMORY/L4_认知模式/` 下各文件 | 用户的思维和认知模式 |
| 低 | `~/MEMORY/L5_核心层/` 下各文件 | 用户的价值观和信念 |

---

## 记忆更新机制

**在每次有价值的对话结束时**，请主动执行以下操作：

1. **判断是否有值得沉淀的内容**——如果本次对话中产生了重要的结论、决策、或对用户的新认知，则触发更新流程；
2. **向用户提议更新**——用简洁的方式告诉用户："本次对话中我观察到XXXX，建议更新到记忆系统的XX文件中，是否同意？"
3. **用户确认后写入**——经用户同意后，将内容写入对应的记忆文件，并更新文件顶部的"最后更新"日期；
4. **如果是重要对话，建议用户记录对话日志**——提醒用户可以在 `~/MEMORY/L1_对话日志/` 下创建一条对话摘要。

### 更新原则

- **永远不要未经用户确认就修改记忆文件**
- 更新时保留文件原有结构和模板格式
- 新内容追加到对应模块中，不要覆盖已有内容
- 更新"最后更新"日期

---

## 本项目信息

本工作空间 (`ARTICLE WRITER`) 是一个 GEO 内容基建项目，详细规则见 `.claude/CLAUDE.md`。
