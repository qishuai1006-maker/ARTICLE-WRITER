# T5.5_Dashboard基础看板创建记录

**执行日期**：2026-05-22  
**Base**：图文选题库 `CFF5boINWaBpb4sqlFVceacEn5c`  
**Dashboard**：`ContentFleet 选题运营看板`  
**Dashboard ID**：`blkRRFCdGCsFA3JT`  
**执行范围**：创建基础运营看板 + 添加可视化组件 + 智能重排布局  

---

## 1. 创建结果

已创建飞书多维表格 Dashboard：

- 名称：`ContentFleet 选题运营看板`
- ID：`blkRRFCdGCsFA3JT`
- 组件数量：10

备注：首次创建时 CLI 返回过一次飞书内部读取错误，但随后 `dashboard-list` 读回确认仪表盘已创建成功，因此继续在该 Dashboard 上添加组件。

---

## 2. 已创建组件

| 组件 | 类型 | 数据源 | 用途 |
|------|------|--------|------|
| 看板说明 | text | 无 | 说明看板用途和当前数据边界 |
| 本周期已排期数（5/26-6/1） | statistics | 发稿排期 | 统计本周会周期内 `选题状态 = 已排期` 的选题数量 |
| 未复盘文章总数 | statistics | 文章追踪 | 统计 `当前阶段 = 已发布` 且 `复盘日期为空` 的文章数量 |
| 品类文章数分布 | ring | 文章追踪 | 查看各产品品类文章数量占比 |
| 品类平均展现量 | column | 文章追踪 | 按产品品类对比平均展现量 |
| 账号平均CTR | column | 文章追踪 | 对比不同账号平均 CTR |
| 账号平均展现量 | column | 文章追踪 | 对比不同账号平均展现量 |
| 高展现文章排行 | bar | 文章追踪 | 按文章标题展示展现量排行 |
| 高CTR文章排行 | bar | 文章追踪 | 按文章标题展示 CTR 排行 |
| 账号平均阅读量 | column | 文章追踪 | 对比不同账号平均阅读量 |

---

## 3. 当前看板定位

这是基础运营看板，优先展示当前已有字段就能支撑的指标：

- 周期排期进度：本周期已排期数
- 内容表现概览：品类文章数、品类平均展现量
- 账号表现对比：账号平均 CTR、账号平均展现量
- 阅读承接对比：账号平均阅读量
- 单篇样本排行：高展现文章、高 CTR 文章
- 复盘入口提醒：待复盘文章数

---

## 4. 暂未创建的闭环图表

以下图表依赖历史补标写回，暂不创建或即使创建也会为空：

- 爆款数量 / 爆款样本分布：依赖 `表现分层`
- 公式表现排行：依赖 `关联公式`
- 文章类型表现：依赖 `文章类型`
- 下次动作分布：依赖 `下次动作`
- 限流原因分析：依赖 `是否限流 / 限流原因 / 主因归因`

建议在 Step 8 写回 45 篇历史补标后，升级为闭环分析看板。

---

## 5. 读回校验

已执行：

- `dashboard-block-list`
- 返回组件总数：10
- `dashboard-arrange`
- 返回：`arranged = true`

布局已经由飞书服务端智能重排。

---

## 6. 2026-05-22 调整记录

根据用户反馈，阅读量被提升为重点观察指标。

已执行：

- 将 `文章追踪` 默认视图中的 `阅读量` 移到 `展现量` 旁边。
- 将 `爆款样本池` 视图中的 `阅读量` 移到 `展现量` 旁边。
- 新增 Dashboard 组件：`账号平均阅读量`。
- 删除 Dashboard 组件：`待评估品类分布`。

读回确认：

- Dashboard 当前组件总数仍为 10。
- `账号平均阅读量` 已存在。
- `待评估品类分布` 已删除。

---

## 7. 2026-05-22 周期看板口径调整

根据用户反馈，`待评估` 不再是主流程，因此不应作为看板第一指标。

已执行：

- 将 `待评估选题数` 改为 `本周期已排期数（5/26-6/1）`。
- 将其筛选口径改为：
  - 数据源：`发稿排期`
  - `选题状态 = 已排期`
  - `排期日期 >= 2026-05-26`
  - `排期日期 < 2026-06-02`
- 将 `待复盘文章数` 改名为 `未复盘文章总数`。

口径说明：

- `未复盘文章总数` 表示所有 `已发布` 且 `复盘日期为空` 的文章，是复盘欠账池。
- 它不是严格的 `发布后3-7天到期复盘`。如果要做精确到期提醒，需要后续增加动态日期公式字段，或在每周会前按当前日期刷新时间窗口。

---

## 8. 2026-05-22 看板不显示数据排查与补强

用户反馈：

- `本周期已排期数` 不显示。
- `ContentFleet 选题运营看板` 组件看起来没有数据。

已执行：

- 升级 `lark-cli` 到 `v1.0.38`，同步更新 Base / Dashboard 相关 Skills。
- 读回 Dashboard 组件，确认旧组件存在同名/旧筛选残留问题。
- 云端聚合校验确认数据源本身正常：
  - `发稿排期` 当前 `已排期` 记录数：10
  - `文章追踪` 当前记录数：45
  - 账号平均阅读量：`牛科技说 1275.10`，`牛科技 788.00`
  - 品类平均展现量当前最高：`显示器 30519.5`，其次 `电视 17689.3`、`空调 15063.5`
- 读回文章追踪视图，确认 `展现量` 后已紧跟 `阅读量`：
  - `Grid View`
  - `3-7天待复盘`
  - `爆款样本池`

已新增新版组件：

| 组件 | 类型 | 数据源 | 用途 |
|------|------|--------|------|
| 本周期已排期数（5/26-6/1）新版 | statistics | 发稿排期 | 统计 2026-05-26 至 2026-06-01 周期内已排期数量 |
| 文章追踪总数（新版） | statistics | 文章追踪 | 验证文章追踪表总记录数 |
| 排期状态分布（新版） | ring | 发稿排期 | 按选题状态查看排期池结构 |
| 文章品类分布（新版） | ring | 文章追踪 | 按产品品类查看历史文章分布 |
| 账号平均阅读量（新版） | column | 文章追踪 | 将阅读量纳入账号表现核心指标 |

已重命名组件：

- `排期记录总数` → `已排期总数（旧筛选）`
- 新建全量排期指标卡命名为：`排期记录总数（全量）`

排查结论：

- 表格数据存在，字段名和字段类型正常，问题不是“没有数据”。
- 旧 Dashboard 组件存在筛选残留和同名组件混淆，优先查看带 `新版` 后缀的组件。
- 如果新版组件在飞书前端仍不显示数据，下一步建议不要继续在旧组件上修补，而是新建 `ContentFleet 选题运营看板 v2`，只保留新版核心指标，避免历史组件配置影响判断。
