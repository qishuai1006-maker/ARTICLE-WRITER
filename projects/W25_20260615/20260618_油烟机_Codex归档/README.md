# Codex Outputs

这里是 Codex 在当前工作流中的独立产出区。

## 规则

- Codex 生成的文章、证据卡、配图卡、图片、Word 统一放在这里。
- 不把 Codex 产物放在 `outputs/` 根目录，避免和 ZCode、Claude Code 或其他智能体混在一起。
- 正式图片必须真实落盘到本目录，并通过 `Scripts/validate_visual_outputs.py` 验收。
- 对话预览图不算正式图片，不能写进 Word 或最终 Markdown。
