# Super Writer · 生产 SOP 全流程

> **定位**：图文生产的**唯一入口 SOP**。任何账号 / 品类 / 会话开工，先读这一份。把散落在 `CLAUDE.md` / `Agents/` / `系统日志.md` / `Skills/` / `codex-agents/` 的流程收敛成一张可勾选清单。
> **版本**：v1.0（2026-06-21，CEO 要求"完完整整顺一遍全部生产步骤"。固化选题决策/数据调研/正文写作/审核/生图/发布/复盘 7 环节闭环 + 断点检查 + 脚本索引）
> **与现有文档的关系**：
> - `CLAUDE.md` = 核心调度字典（规则/红线/Context Tiers 指针）
> - `Agents/01-04` = 四棒各自职责（怎么想的深度）
> - `Skills/` = 能力包（怎么写的细节）
> - `系统日志.md` = 多智能体协作中枢（变更/状态）
> - **本 SOP = 流程入口（按什么顺序做、每步交什么产物、卡在哪道闸）**
> **铁律**：本 SOP 是流程层闸门的唯一真相源。任一环节的必产物缺失 = 流程不完整 = 不得进入下一棒。叙述性"我做过了"不算数，**只认落盘文件 + 状态卡**。

---

## 〇、总览：7 环节闭环

```
①选题决策 → ②数据调研 → ③正文写作 → ④审核 → ⑤生图 → ⑥发布 → ⑦复盘 → 反哺①
   (01)       (01.5)       (02)       (03)    (04)   (CEO)   (/fupan)
```

| 环节 | 负责棒 | 必产物 | 关键闸门 | 自动化 |
|---|---|---|---|---|
| ① 选题决策 | 01 情报拆解 | **选题方案表（CEO 点头）** + 对标母本卡 + 爆款拆解卡（双闸门+内核变异）| 选题方案未点头→不进拆解；信息增益填不出→枪毙；撞车→打回 | 半自动（撒网+撞车脚本，CEO 点头手动）|
| ② 数据调研 | Claude Code（首选·主控亲跑）| `L3_场景调研报告/..._L3调研报告.md`（11 节弹药包）| 5 必查 + 8 项写前闸门 | ✅ 自动（web_search_prime + webReader；主控亲跑）|
| ③ 正文写作 | 02 主笔 | 02 初稿 + 02b 证据卡(召回清单) + 自检清单(流量卡)| 召回清单缺/流量卡空→打回 | 自动 |
| ④ 审核 | 03 风控主编 | 03 终稿 + `lint PASS` | 红线/事实错/召回审计不过→打回 | 自动（lint 脚本）|
| ⑤ 生图 | 04 视觉 | 04 配图说明 + T5 落盘图 + visual 验证 | 图未落盘不得进 Word | 半自动（生图模型+HTML 兜底）|
| ⑥ 发布 | CEO | 头条/知乎上线 | 默认 `--draft`，CEO 手动发 | 手动 |
| ⑦ 复盘 | CEO + /fupan | 归档复盘卡 + 公式库升降级 | 发布 3-7 天必填 | 半自动（/fupan 命令化）|

> **闭环铁律**（CLAUDE.md 原话）：不填复盘卡 = 闭环断在最后一步。⑦ 反哺① 是"开环生产"变"爆款复利机器"的唯一开关。

---

## ① 选题决策（01 情报拆解）

**负责**：01 情报拆解 Agent（Claude Code 或 Codex topic-agent）
**输入**：CEO "开工/今天写"（默认）/ 偶尔下发对标文章（已在 `对标文章归档/`）

**动作**（选题方案表在先，拆对标在后 · CEO 只说开工时两步都要走）：
0. **出选题方案表**（CEO 只说开工时必做 · CEO 点头才进步骤 1）：按 `Skills/08-选题方案表模板.md` 撒 5 角度（≥2 母体 × ≥2 评论发动机）→ 跑 `Scripts/check_topic_collision.py` 撞车预检 → 选定 1 个（≥3 条数据支撑理由）→ 核心抓手 ≥3，落盘 `outputs/[账号]_[品类]_[日期]/00_选题方案表.md`，**CEO 点头/微调后才进步骤 1**。CEO 已直接下发对标+品类可跳过本步。Claude Code 走 `/write` Step 0.5。
1. 拆对标 → 产**对标母本卡**（长期复用资产，记派生台账）
2. 产**爆款拆解卡**，必须填齐：
   - **闸门 A · 信息增益**（决定给不给展现）：1-3 条"以为 A 其实 B"非百科独家判断，每条标证据来源（指向 L1/推品手册，不硬编码参数值）。填不出 → 选题枪毙。
   - **闸门 B · 流量卡 5 项**（决定点不点/留不留）：①15 字点击钩子 ②首屏冲突 ③反常识判断 ④收藏死线 ⑤评论触发点。
   - **内核归类 + 变异**（防同质化）：按 `运营方法论/04_爆文内核库.md` 归 1-2 内核；定主证据对象（不许和本号近 14 天同品类重复）；产出变异 ×3。
3. **撞车检查**：跑 `Scripts/check_topic_collision.py <账号> <品类> [关键词] [--title "候选标题"]`。同核心对立已做过且上次展现 >1 万 → 打回或强制换锐角。

**必产物**（落盘）：
- `对标拆解库/对标母本卡_[关键词]_[日期].md`
- `对标拆解库/爆款拆解卡_[品类]_[日期].md`（含双闸门 + 内核变异 + 撞车结果）

**闸门**：信息增益 FAIL / 撞车报警 → 选题打回，不进 ②。
**断点检查**：拆解卡未落盘 → 不得进 ②。（Claude Code 双号生产历史跳过 01，现强制落盘，见 §八状态卡统一）

---

## ② 数据调研（01.5 · 新增 · 固化 CEO 手动动作）

> **为什么独立成节**：CEO 早期手动用 Gemini/ChatGPT 针对当天选题跑数据调研（销量/评论/价格/竞品），这个动作此前没固化进流程，"现在没有了"。本节把它升级为**独立可检查产物**（呼应系统日志"叙述性→可检查产物"铁律）。

**负责**：**Claude Code 自动（首选）** / CEO 手动兜底
**输入**：① 的拆解卡 + 目标品类

**必查 5 项**（落盘进 `01c`）：
| # | 必查项 | 用途 |
|---|---|---|
| 1 | **销量梯队**（该品类/型号在京东/抖音销量级别）| 判断热度 + 选推品手册哪档型号 |
| 2 | **价格区间**（当前国补后实测价，CEO 京东核实口径）| 价格跨度/后果冲突的弹药（闸门 B 首屏）|
| 3 | **用户痛点评论**（差评/追评里高频抱怨什么）| 信息增益 + 评论触发点的真实素材 |
| 4 | **竞品爆款**（同品类别人怎么写的，TOP3 标题/结构）| 防撞车 + 找差异化锐角 |
| 5 | **热度信号**（近期是否上升/季节性）| 选题时效判断 |

**自动化双路径**（⚠️ v1.0 初版方案，CEO 确认定稿）：

- **路径 1 · contentfleet MCP（首选·激活后）**：
  - `product_search`（先 `pinlei_list` 查品类标识）→ 型号 / 参数 / 参考价 / 推荐等级
  - `tavily_search` → 评测/口碑交叉验证关键参数
  - `douyin_search`（仅信号源非证据源）→ 品类/型号近期热度
  - **前置**：MCP 需批准激活（当前 `⏸ Pending approval`）。
- **路径 2 · `mcp__web-search-prime__web_search_prime` + `mcp__web_reader__webReader`（兜底·实测可用 2026-06-22）**：
  - `mcp__web-search-prime__web_search_prime` 搜 "[型号] 销量/价格/差评"、"[品类] 选购 爆款"、"[型号] vs [竞品]"
  - `mcp__web_reader__webReader` 抓京东商品页 / 评测文 / 头条爆款正文
  - ⚠️ **禁用 `WebSearch` / `WebFetch`**——它们是 deferred 工具，子 agent 独立上下文不先 ToolSearch 加载直接调必报错卡死（见 `/write` Step 0 防卡死铁律 #1）。主控或子 agent 都走上面两个 MCP。
  - 适用：MCP（contentfleet）未激活 / 查不到的维度。
- **Claude Code 替代 Gemini 的分析活**：MCP/WebSearch 拿硬数据后，**我来做分析**——选题角度发散（复刻早期"AI 撒 5 个角度"）、痛点提炼、信息增益判断、竞品差异化锐角。等于把你手动跑 Gemini 的分析环节内化进闭环。
- **手动兜底（保留）**：仅当 MCP + WebSearch 都拿不到（后台销量 / 付费研报 / 你想要 Gemini 某次深度追问），CEO 手动跑 Gemini/ChatGPT，结果贴回，Claude Code 整理落盘。

**必产物（主产物落 L3 · CEO 0622 定）**：`家电知识库/L3_场景调研报告/YYYYMMDD_牛科技或牛科技说_品类_选题关键词_L3调研报告.md`——用 `家电知识库/_templates/L3_调研报告_模板.md`（11 节弹药包：选题判断 / 观点候选 / 痛点清单 / 参数翻译表 / 型号价格对比表 / 信息增益清单 / 评论钩子 / 标题方向 / 封面建议 / 事实风险 / 写前闸门自检）。`outputs/[账号]_[品类]_[日期]/` 不再堆 01c 临时文件；状态卡"必召回 L3 场景调研"字段直接填 L3 路径（下游校验靠此字段非空，不靠 project_dir 里有 01c 文件）。
**闸门（双闸）**：① **数据采集闸**——5 项必查（销量/价格/痛点/竞品/热度），查不到标缺项 + 是否影响信息增益；② **弹药包闸**——L3 模板第 11 节 8 项写前闸门自检（3 可信来源 / 5 信息增益 / 参数翻译表 / 型号价格证据 / 痛点场景 / 明确观点非中庸 / 评论钩子 / 事实风险）全"是"，缺任一项回 ① 调选题或补查，不得带缺口进 ③。
**断点检查**：L3 调研报告未落盘 / 8 项写前闸门未全"是" → 不得进 ③。

---

## ③ 正文写作（02 首席主笔）

**负责**：02 首席主笔 Agent
**输入**：① 拆解卡 + ② `01c` 数据调研 + 目标品类

**动作**（按顺序）：
1. **召回（动笔前必做 · 留痕进证据卡「召回清单」节）**：
   - `推品逻辑手册_2026.md` → 首推型号（唯一型号来源，禁瞎编）
   - `对标拆解库/品类弹药包/[品类].md`（TOP 爆款钩子/限流反面/活跃公式，`gen_category_ammo.py` 刷新）
   - `家电知识库/L1_品类技术原理`（技术深度+反常识）+ `L4_达人选购策略`（避坑/观点/争议型主弹药）+ `L2_品牌档案`（涉品牌）+ `L3_场景调研`（按需）
   - `运营方法论/05_家电参数翻译库.md` → **本品类 ≥3 条参数翻译**（不足先补库再写）
   - ⚠️ 召回卡片 frontmatter `Valid_Until` 过期 → MCP 核对从"建议"升级"必做"
2. **MCP 实时验证**（激活后）：`product_search` 核型号/价格，`tavily_search` 交叉口碑。写进正文仍走证据卡登记。
3. **流量卡 5 项**（动笔前必过，任一空不得动笔）。
4. 写初稿（标题 25-29 字 + 强制悬念/反问；首段 ≤50 字；预留 2 处信息图占位符）。
5. 去 AI 味（`Skills/04` 白名单 + Humanizer-zh 复核，只改语气不改型号/参数/价格）。
6. 填证据卡（所有型号/参数/价格/标准进表；`UNSURE` 不进正文；`SINGLE_SOURCE` 加边界）。

**必产物**：
- `02_首席主笔初稿_[品类].md`
- `02b_轻量证据卡_[品类].md`（含召回清单 L1/L2/L4/L3 + 主证据对象）
- 自检清单（标题字数 + A 类禁词 0 命中 + 流量卡 5 项 + 推品手册引用型号数）

**闸门**：召回清单缺失 / 流量卡任一空 / 参数库 <3 条 → 打回，不得进 ④。
**断点检查**：证据卡召回清单空 → 不得进 ④（凭记忆"召回过"不算数）。

---

## ④ 审核（03 风控主编）

**负责**：03 风控主编 Agent（终审一票否决）
**输入**：02 初稿 + 02b 证据卡 + 自检清单

**动作**：
1. 红线扫描（`Skills/06` A 类禁词 / 绝对化用语 / 违规承诺）
2. 事实核对（正文数字/型号/价格 vs 证据卡 100% 吻合，不符=编造→打回）
3. 来源泄露检查（正文出现"推品手册/手册里/证据卡/内部资料"等→打回）
4. 虚假公开来源检查（写"官网/商品页"但证据卡无对应公开来源→打回）
5. 流量闸门 5 项复审（标题/首屏/反常识/收藏死线/评论触发）
6. **召回闭环审计**：证据卡召回清单是否填、L4 避坑型是否召回、≥1 个 L1/L2/L4 硬事实是否进标题/首屏/前 300 字、是否形成主证据对象
7. 去 AI 味 + 排版（段落长短交替，禁连续 3 段同字数）复核
8. `Scripts/lint_quick_article.py`（前 300 字硬实体密度 <2 个→ERROR）

**必产物**：`03_风控主编终稿_[品类].md` + `lint PASS`
**闸门**：红线/事实错/召回审计不过 → 打回 02，不得以"完整/lint 通过"放行。
**Hook**：编辑/生成 `03_风控主编终稿` 后自动触发 lint 验证，报错原地修。

---

## ⑤ 生图（04 视觉指挥官）

**负责**：04 视觉指挥官 Agent
**输入**：03 终稿 + 02b 证据卡 + 流量卡 + 召回结果（含允许上图数字/型号/单位）

**动作**：
1. content → image 交接卡闸门（缺流量卡/召回清单/主证据对象/允许上图字段 → 打回上游）
2. `04_视觉指挥官配图_[品类].md`：每图标 文件名/类型/功能(点击·理解·收藏·对比)/服务流量卡/知识库素材/插入位置/数据来源/工具/验收标准 + 完整文生图提示词
3. **优先生图模型**（image-2/ChatGPT/Codex 生图）窗口预览，确认后落盘
4. **兜底**：参数密集/数字零误差的信息图走 NotebookLM CLI / HTML→Chrome（`render_html_to_png.py`）；**封面不用 NotebookLM（只出扁平图），封面要真实生图 → 给 CEO 即梦/GPT-4o 提示词**
5. 落盘到文章目录，`Scripts/validate_visual_outputs.py` 验证（命名/分辨率/文件大小/配图说明引用）+ 肉眼验收（无 vision MCP，靠 CEO 核 + source 零数字降险）
6. 重建 Word（`build_article_docx.py`），只嵌已落盘已验收图

**必产物**：04 配图说明 + `T5_*.png`（落盘）+ visual 验证通过 + Word 成品 + `06_发布交付清单`
**闸门**：图未落盘 / visual 验证不过 → 不得进 Word / 终稿。
**铁律**：Pillow/PIL/matplotlib 只能草稿，不作正式成品；未落盘图不进 Word。

---

## ⑥ 发布

**负责**：CEO（手动）
**动作**：CEO 审最终 docx → 头条/知乎手动发布。
**铁律**：`publish_toutiao.py` 默认带 `--draft`，**不擅自真实发布**。
**产物**：发布链接（供 ⑦ 复盘用）。

---

## ⑦ 复盘（数据飞轮 · 闭环最后一步）

**负责**：CEO 触发 + `/fupan` 命令 + Claude Code 落盘
**时机**：发布 3-7 天数据稳定后

**动作**：
1. `/fupan <归档目录> <头条链接>` → 填 `归档复盘卡`（表现分层 + 归因 + 下次动作）
2. `/import <头条Excel>` → 飞书批量导入表现数据
3. `Scripts/loop_status.py` → 看板（标待复盘/已复盘/限流）
4. 据"下次动作"驱动 `运营方法论/02_爆款公式库.md` 公式升降级
5. 复盘结论反哺下一轮 ① 拆解与标题

**闸门**：`/write` Step 1 强制——公司号有待复盘超期 → 先提示 CEO 跑 `/fupan` 再生产（不开盲区稿）；必须引用 ≥1 条近期复盘教训。
**断点**：当前复盘闭合率 **0%**（loop_status 实测）= 最大未闭合环节。

---

## 八、状态卡统一（双轨合一 · 断点修复）

> **问题**：Codex 多 Agent 有 `00_生产状态卡`（S0-S5 阶段），Claude Code 双号生产（如洗衣机横评 0619）**裸跑无状态卡** → 01 被跳、流程不可追溯。

**统一规则**：
- **所有生产**（Claude Code 双号 + Codex）开工先建 `outputs/[账号]_[品类]_[日期]/00_生产状态卡.md`（模板见 `codex-agents/生产状态卡_模板.md`）。
- 状态卡是**唯一真相源**：S0 选题 → S1 数据调研 → S2 正文 → S3 质检 → S4 配图 → S5 Word。下游只认状态卡 + 落盘文件，不认口头完成。
- 每棒完成回填本阶段状态 + 下游交接卡（`topic→content` / `content→image` / `image→publish`）。

---

## 九、断点检查清单（每次生产前必过）

- [ ] **contentfleet MCP 激活？**（数据调研首选；未激活则用 `mcp__web-search-prime__web_search_prime` + `mcp__web_reader__webReader` 兜底——**禁 WebSearch/WebFetch**，deferred 卡死子 agent）
- [ ] **00 生产状态卡已建 + Step 7.5 lint PASS？**（双号也强制；4 项查：唯一/标题一致/阶段↔文件/10 字段）
- [ ] **① 选题方案表 CEO 已点头？**（`outputs/.../00_选题方案表.md` 落盘 + CEO 点头/微调记录；CEO 直接下发对标可跳过但状态卡需注明）
- [ ] **① 拆解卡落盘？**（双闸门 + 内核变异 + 撞车结果）
- [ ] **② 数据调研落盘？**（L3 调研报告在 `家电知识库/L3_场景调研报告/`；5 必查 + 8 项写前闸门自检全"是"）
- [ ] **③ 召回清单完整？**（推品手册+弹药包+L1/L2/L4+05 库 ≥3 条）
- [ ] **④ lint PASS？**
- [ ] **⑤ 图全部落盘 + visual 验证？**
- [ ] **⑦ 上一篇已复盘？**（loop_status 无超期待复盘）

---

## 十、脚本索引

| 脚本 | 环节 | 用途 |
|---|---|---|
| `check_topic_collision.py` | ① | 撞车检查（同内核/标题前 15 字）|
| `gen_category_ammo.py` | ③ | 刷新品类弹药包（从飞书）|
| `lint_quick_article.py` | ④ | 骨架 + 合规 + 前 300 字硬实体密度 |
| `check_title_delivery.py` | ④ | 标题承诺 vs 正文兑现（堵空洞化；`/write` Step 4.5/5 + hook 自动跑）|
| `lint_status_card.py` | ⑦.5 | 状态卡唯一/标题四件套一致/阶段↔文件/CLAUDE.md 10 字段（`/write` Step 7.5）|
| `validate_visual_outputs.py` | ⑤ | 配图落盘/分辨率/大小/引用验证 |
| `render_html_to_png.py` | ⑤ | 信息图 HTML→Chrome 兜底渲染 |
| `build_article_docx.py` | ⑤ | 重建 Word（只嵌落盘图）|
| `loop_status.py` | ⑦ | 复盘看板（待复盘/已复盘/限流）|
| `fetch_article_stats.py` | ⑦ | 抓头条数据 |
| `import_toutiao_stats.py` | ⑦ | 头条 Excel→飞书批量导入 |
| `scan_banned_brand_consistency.py` | ④ | 禁推品牌一致性扫描 |

**命令**：`/write`（生产，已删骨架确认点全程自动到终稿）/ `/fupan`（复盘）/ `/import`（数据导入）

---

## 十一、归因排查速查（扑街时按此顺序）

| 症状 | 先查 |
|---|---|
| 展现低 | 闸门 A（硬实体密度/信息增益/召回），**不是先怪标题** |
| 展现高点击低 | 标题钩子（15 字）|
| 点击高读完低 | 首屏兑现 + 中段硬实体密度 |
| 读完高互动低 | 收藏死线 + 评论诱因 |

---

*本 SOP 是流程层闸门的唯一入口。规则细节看 `CLAUDE.md`，每棒怎么想看 `Agents/`，怎么写看 `Skills/`，变更/状态看 `系统日志.md`。任一环节必产物缺失 = 流程不完整 = 不得进入下一棒。*
