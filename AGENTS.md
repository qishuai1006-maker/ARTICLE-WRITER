# Super Writer v0.1 · 执行层守则

> **最后更新**：2026-06-11

## 一、最高死命令

**执行层绝对禁止跳步**——所有生产必须严格按 T1 → T1.5 → T2 → T3 → T4 流程：

1. 不许跳 T1 第一追问（必须 5 角度撒网）
2. 不许跳 T1.5 骨架选型（必须显式标注骨架）
3. 不许跳 T2 三层深度（推完才能写正文）
4. 不许跳 T3 第三轮（必须喂语调样本）
5. 不许跳 T4 自检（12 项必查）

## 二、核心 Prompt 路径

| 阶段 | Prompt 文件 |
| --- | --- |
| T1 选题 | `prompts/01-选题/选题_三追问.md` |
| T1.5 骨架选型 | `prompts/02-骨架选型/骨架选型速查表.md` |
| T2 深度 | `prompts/03-深度/深度_三追问.md` |
| T3 写作 | `prompts/04-写作/写作_骨架填空模板.md` |
| T4 自检 | `prompts/05-自检/发布前自检清单.md` |

**开工前必读** `prompts/README.md` 第一节（设计哲学）+ `CLAUDE.md` 第一节（定位）。

## 三、产出落盘规范

所有产出写到 `test_run/`：

```
test_run/
├── 选题_第X篇.md        ← T1 + T1.5 + T2 全部记录
└── 成品_第X篇.md        ← T3 成品正文 + T4 自检结果
```

文件名规范：
- 单篇：`选题_2026-06-XX_[简短话题].md`
- 双篇：`选题_2026-06-XX_[话题A]_双篇.md`

## 四、强制质检脚本

```bash
# T3 完成后跑（必跑）
python3 Scripts/lint_article.py test_run/成品_第X篇.md --json

# T4 自检时跑（必跑）
python3 Scripts/validate_outputs.py --verbose
```

退出码：
- `0` = 通过
- `1` = ERROR（必须修）
- `2` = WARNING（建议修）

## 五、上下文管理铁律（v1.0 核心）

> **背景**：v7.2 时代"单篇上下文就满"是因为把 7k+ Prompt 规则灌进对话。v1.0 彻底解决：**规则在文件系统，对话只 cat 引用**。

### 铁律 1：规则引用而非复制

**禁止**：
```
❌ 把 prompts/01-选题/选题_三追问.md（7,718 字）整个粘贴进对话
```

**必须**：
```
✅ Claude Code:  @/Users/aaron/.../prompts/01-选题/选题_三追问.md
✅ Hermes:       用 Read 工具读取文件
```

### 铁律 2：每 T 一对话

| T | 对话开头话术 |
| --- | --- |
| T1 | "读取 test_run/[完整路径]（如不存在则新建），按 prompts/01-选题/选题_三追问.md 跑 T1。" |
| T1.5 | "读取 test_run/[完整路径] 的 T1 摘要，按 prompts/02-骨架选型/骨架选型速查表.md 选骨架+标题。" |
| T2 | "读取 test_run/[完整路径] 的 T1+T1.5 摘要，按 prompts/03-深度/深度_三追问.md 推 3 层。" |
| T3 | "读取 test_run/[完整路径] 全部记录 + assets/titles/[对应品类+模板].md，按 prompts/04-写作/写作_骨架填空模板.md 写正文。" |
| T4 | "读取 test_run/[完整路径] + test_run/成品_[完整路径]，按 prompts/05-自检/发布前自检清单.md 跑 12 项检查。" |

### 铁律 3：进度标记 + YAML 接力

每篇 `选题_*.md` 末尾自动追加进度：

```markdown
## 进度
- [x] T1 选题（5 角度 + 选定）  ← 2026-06-11 09:30
- [x] T1.5 骨架选型 + 标题      ← 2026-06-11 09:45
- [x] T2 三层深度               ← 2026-06-11 10:15
- [ ] T3 写作（进行中）         ← 2026-06-11 10:30
- [ ] T4 自检
```

每完成一个 T，写 `test_run/_handoff/[文章ID]_T[阶段].yaml`（见 `_protocols/handoff_template.yaml`）。

### 铁律 4：上下文快满的 3 个信号 + 应对

| 信号 | 应对 |
| --- | --- |
| AI 开始重复自己（"如前所述..."超过 3 次） | `/new` 开新会话 |
| AI 引用错的"前文"（你没说过的话） | **立刻停止**，开新会话 |
| AI 输出质量明显下降（标题超 29 字、骨架漏步） | 开新会话 + 备份当前最好的部分 |

### 5 分钟恢复步骤（任何断点）

1. `ls -lt test_run/选题_*.md | head -3` — 看最新的
2. `cat test_run/选题_[最新].md` — 看进度
3. 新会话开头用上面的"对话开头话术"

详细见 `_protocols/cross_session_resume.md`。

## 六、绝不做的事

1. ❌ 自行选题不通过三追问
2. ❌ 修改选题方向（必须回 T1 重做）
3. ❌ 跳过骨架五步中的任何一步
4. ❌ 不喂语调样本就用 AI 默认口吻
5. ❌ 不跑 lint_article.py 就提交发布
6. ❌ 在 outputs/ 之外的地方写正文
7. ❌ 草稿模式自动发布（必须 CEO 二次确认）

## 七、参考资料

- **核心章程**：`CLAUDE.md`
- **核心 Prompt**：`prompts/README.md`
- **合规清单**：`Skills/04-合规与质量双审Skill.md`
- **活人写手感**：`Skills/10-活人写手感.md`
- **选题域白名单**：`Skills/01-选题情报Skill.md`
- **产品库**：`Skills/产品信息库技能.md`