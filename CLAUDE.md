# Super Writer · 核心调度字典

> **核心哲学**：不造轮子，只做降维打击。
> **版本**：v4.0（精简架构 & 记忆外挂版）

## 🎯 Project Overview
- **目标**：打造能够批量制造爆款的图文起号流水线（头条/知乎等）。
- **架构**：全系统由四大专职 Agent（拆解、主笔、主编、视觉）接力协作。
- **验证标准**：产出的文章必须能在不加任何修改的情况下，通过 `Scripts/lint_quick_article.py` 质检并在头条获得高点击率。

---

## 🚫 Do NOT introduce (坚决抵制清单)
Unless explicitly requested, **不要**引入以下行为：
- **拒绝单轮直出**：严禁在一次对话中跳过拆解、骨架确认等环节直接生成正文全文。必须遵循接力工作流。
- **拒绝外部网络大模型 API**：所有 LLM 生成逻辑必须依靠当前的 Claude Code 闭环执行，不要写调用外部 API 的脚本。
- **拒绝复杂架构**：不要引入 Python 爬虫框架（如 Scrapy）、Web 框架。所有脚本保持单文件、无额外依赖（标准库为主）。
- **拒绝随意改写已有 Agent**：不要为了新功能去破坏 `Agents/` 目录下的职责边界。
- **拒绝擅自真实发布**：涉及 `publish_toutiao.py` 时，默认必须带 `--draft` 参数。
- **拒绝代码图冒充成品**：Pillow/PIL/matplotlib 只能做草稿或排版占位，不能作为正式封面/正式信息图交付。
- **拒绝未落盘图片进终稿**：生图工具只给对话预览但没有本地文件路径时，只能标记为“预览”，不能写入 Word 或最终 Markdown。
- **拒绝消耗式对标**：`对标文章归档/` 里的对标文是母本资产，不得对标完就删除，也不得把原文直接交给主笔贴句改写。

---

## 🧠 Memory Loop
`MEMORY.md` 是你（Claude Code）的跨会话长期记忆中枢。
- 每次新任务**开始前**，先读取 `MEMORY.md` 提取我（CEO）最近的偏好、易踩坑点和高转化套路。
- 每次任务**结束后**，如果有新的爆款文案方法、我新补充的绝对红线或新厌恶的废话词汇，**必须更新到 `MEMORY.md` 中**。

---

## 👨‍💻 My Working Style (我的偏好)
- **直接给方案**：先告诉我你的判断或给出一个骨架，不要一开始就塞满代码或几千字正文。
- **中英双语规范**：所有对话回复用**中文**；所有的代码、脚本注释、系统命令输出保持原样（最好英文）。
- **杜绝废话**：回复中不要带 "Great question!", "I'd be happy to help!"、"好的，我马上处理" 这类废话。
- **不确定的必须问**：遇到推品手册里没有的品类，或者对标文章看不懂，停下来问我，不准瞎编参数！

---

## 📏 Coding & Writing Rules (硬规则)
- 标题生成：必须控制在 **25-29 字**，且强制包含悬念/反问，不能直白平铺。
- **双闸门优先于交付完整性**：`lint PASS / Word 生成 / 图片落盘` 只是发布底线，不是完成充分条件。每篇正文动笔前必须先过两道闸，再进入正文、作图或 Word：
  - **闸门 A · 算法初筛（决定给不给展现）**：①前 300 字硬实体密度——lint 已强制，型号/带单位参数 <2 个直接 ERROR（见 `Scripts/lint_quick_article.py`）；②信息增益——拆解卡必须填 1-3 个读者搜不到的非百科独家判断（“以为A其实B”），填不出选题枪毙（见 `Agents/01` D 节）。
  - **闸门 B · 用户行为（决定点不点、留不留，原 5 项流量卡）**：①15字点击钩子，②首屏冲突，③反常识判断，④收藏死线，⑤评论触发点。
  - **归因顺序（扑街时按此排查）**：展现低 → 先查闸门 A（实体密度/信息增益，不是先怪标题）；展现高点击低 → 查标题钩子；点击高读完低 → 查首屏兑现 + 中段密度；读完高互动低 → 查收藏死线 + 评论诱因。
  - **中段密度红线**：文本密度指“硬实体（型号/参数/价格）密度”，与开头人味正交——开头抓人（人味）→ 中段喂硬实体 → 结尾给决策。**别为提密度砍人味，也别为保人味牺牲中段实体**。TCL 电视好稿（前 300 字 9 个硬实体 + 人味钩子，2.3 万展现）是范本。
- **内核优先于形式（反同质化）**：拆解爆款时复用的是「内核」（6 类人性驱动力，见 `运营方法论/04_爆文内核库.md`），不是「形式」（标题结构，用多了会疲劳——实测同形式 8 次从 13.9 万跌到 59）。铁律：**内核可继承、形式必须换、具体性必独家**。01 拆解前必跑 `Scripts/check_topic_collision.py` 查撞车；同内核不许套本账号最近用过的形式。
- **优先生产 S7 单品牌/型号解码题**：近期实测 `TCL电视型号暗藏玄机？3招看懂Pro和Ultra，别多花冤枉钱` 获得 2.3 万展现。后续同等条件下，优先选择 `{品牌/系列}{品类}型号暗藏玄机？{N}招看懂{后缀/参数}，别多花冤枉钱` 这类“型号迷宫 + 损失厌恶”结构，少写泛泛的“XX怎么选”。
- **解释清楚不等于值得发布**：如果稿子只是把参数解释明白，但没有型号信息差、价格跨度/后果冲突、明确立场和可截图死线，主笔必须主动打回重写，不得包装成“已完成”。
- **Codex 多 Agent 状态卡强制落盘**：每篇 Codex 图文生产必须先有 `outputs/Codex/[账号]_[品类]_[日期]/00_生产状态卡.md`。个人号（宅研社 / 北北ks）日常生产按 `codex-agents/个人号两会话生产线.md` 执行，由 `personal-production-agent` 负责 S0-S3 + S7-S9、`personal-visual-agent` 负责 S4-S6；旧 `content-writing-agent` 已并入 `personal-production-agent`，仅作 legacy 存档或 CEO 单独要求的专项使用。缺 15字点击钩子、首屏冲突、反常识判断、收藏死线、评论触发点、主证据对象、必召回 L1/L2/L4/L3 任一项，必须退回，不得自行脑补；下游只认状态卡和落盘文件，不认口头完成。
- 文章开头：第一段字数不要超过 50 字，最好使用短平快的冲突或痛点引入。
- 事实溯源：所有具体型号参数、价格、卖点，都必须能与 `对标拆解库/轻量证据卡_模板.md` 里的数据对应。
- 排版：正文里必须交替使用不同长短的段落，严禁出现连续 3 段以上字数、行数相同的段落（调用 `07-节奏与情绪引擎` 校验）。
- 去 AI 味：坚决执行 `04-活人写手感规范.md` 里的白名单，拒绝“在这个数字化时代”、“总而言之”、“毋庸置疑”等 AI 惯用词。正文终稿前增加 **Humanizer-zh 去 AI 味复核**（参考 `op7418/Humanizer-zh`）：重点检查破折号滥用、空话套话、机械三段式、广告腔、通用积极结尾、过度粗体/列表。若本地已安装该 Skill，调用 `/humanizer-zh`；未安装时按其 24 类 AI 写作痕迹手动复核。复核只改语气和句式，严禁改丢型号、参数、价格、证据边界。
- 配图生产：先在 `04_视觉指挥官配图_[品类].md` 写出每张图的专业文生图提示词，再优先用内置生图模型（image-2 / ChatGPT/Codex 生图）直接出窗口预览；确认可用后再落盘并验收。HTML/Chrome 渲染只作为生图受阻或强结构化数据图的兜底，不得在同一问题上反复卡死消耗 token。每张图必须说明承担“点击 / 理解 / 收藏 / 对比”中的哪一项。
- 对标文章：先入 `对标文章归档/`，再产出 `对标拆解库/对标母本卡` 和 `拆解卡`。主笔只读取机制卡、证据卡和品类资料，不直接拿原文做正文仿写。

---

## 🗺️ Context Tiers (指针字典)

> **🏛️ 系统日志（多智能体协作中枢）**：变更日志 + 状态看板。  
> **🚨 单会话铁律**：从 Phase 1 起，不再使用多 Agent Teams，所有任务在单一会话内通过角色切换顺序执行。

不要一次性读取所有文档！当你需要做某事时，请去对应的路径寻找：

- **🚀 生产入口（流程层 · 最高优先）**：**执行任何文章生产任务，第一步读 `生产SOP_全流程.md`**——7 环节闭环（①选题决策 → ②数据调研 → ③正文写作 → ④审核 → ⑤生图 → ⑥发布 → ⑦复盘）+ 每步必产物 + 闸门 + §九断点检查清单 + §十脚本索引。它是**流程层唯一入口**：CLAUDE.md 是规则字典，Agents 是每棒职责，本 SOP 串起顺序与闸门。开工先过 SOP §九断点清单。

- **Tier 1 (工作流调度器)**：需要执行文章生成任务时，先看 `Agents/` 目录下的 4 个 Agent。
  - 第一棒：`Agents/01-情报拆解Agent.md`
  - 第二棒：`Agents/02-首席主笔Agent.md`
  - 第三棒：`Agents/03-风控主编Agent.md`
  - 第四棒：`Agents/04-视觉指挥官Agent.md`
- **Tier 2 (能力外挂与资料)**：执行具体写作或核对时读取。
  - 数据源：`推品逻辑手册_2026.md`
  - 家电知识库（技术原理 + 品牌档案 + 场景调研，背景层）：`家电知识库/`（Agent 02 动笔前按品类召回，规则见 `家电知识库/00_索引与召回规则.md`；**不替代推品手册的决策表**，只提供"为什么这么选"）
  - 家电参数翻译库（参数→误解→后果→判断，正文每个参数讲"钱+体验"不讲性能）：`运营方法论/05_家电参数翻译库.md`（主笔动笔前按品类召回 **≥3 条**，否则该品类弹药不足先补库再写）
  - 对标母本原文库：`对标文章归档/`（只供入库、登记、拆解时读取；写稿阶段默认隔离）
  - 对标母本登记模板：`对标拆解库/对标母本卡_模板.md`
  - 选题方案表模板（开工第一产物 · CEO 点头才进拆解）：`Skills/08-选题方案表模板.md`
  - 骨架规范：`Skills/05-牛科技长文原型规范.md`
  - 文风样本：`Skills/style_examples.md`
  - 活人语感：`Skills/04-活人写手感规范.md`
  - 情绪与节奏：`Skills/07-节奏与情绪引擎.md`
  - 禁词库（含平台实测限流词 + 安全词白名单）：`Skills/06-合规红线词库.md`
  - 配图规范：`Skills/03-配图提示词生成规范.md`
  - 对标拆解心法：`运营方法论/01_对标拆解工作流规范.md`
  - 双号策略与结构矩阵：`运营方法论/01_账号定位与双号策略.md`
  - 爆款公式库（结构复利 · 5 态生命周期）：`运营方法论/02_爆款公式库.md`
  - 爆文内核库（反同质化 · 6 大内核 + 复用三原则）：`运营方法论/04_爆文内核库.md`
  - 爆文认知弹药库（标题三法则 / 反直觉规律 / 禁词不钝化）：`运营方法论/03_头条爆文认知清单.md`
- **Tier 3 (历史档案与工具)**：按需查阅。
  - 历史作品：`test_run/`、`projects/`
  - 对标母本派生记录：优先看 `对标拆解库/对标母本卡_*.md` 中的“派生台账”
  - 复盘卡（发布后 3-7 天必填，驱动公式库升降级）：`对标拆解库/归档复盘卡_模板.md`
  - 自动化脚本：`Scripts/`
  - Codex 长期 Agent 登记表：`agent-lanes.md`

---

## 🔗 Hooks & Quality Gates
以下规则由 `.claude/hooks/` 强制执行：
- 每次你编辑或生成 `outputs/` 下的 `03_风控主编终稿` 后，会自动触发验证脚本检查骨架和合规性。如果报错，你必须原地修正。
- 每次进入 Agent 04 或交付 Word 前，必须运行 `Scripts/validate_visual_outputs.py` 检查图片真实落盘、分辨率、文件大小和配图说明引用。

## 🔁 数据飞轮（发布后闭环 · CEO 手动触发）
发布不是终点。每篇文章发布 3-7 天数据稳定后：
1. CEO 填写 `对标拆解库/归档复盘卡_模板.md`（表现分层 + 归因 + 下次动作）。
2. 据"下次动作"驱动 `运营方法论/02_爆款公式库.md` 公式升降级（复用→升级；降权/冻结→降级）。
3. 复盘结论反哺下一轮 01 拆解与标题。
> 这是从"开环生产"变成"爆款复利机器"的唯一开关。不填复盘卡 = 闭环断在最后一步。
