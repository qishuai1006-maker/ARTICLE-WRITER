# Super Writer v1.0 · 头条图文新章程

> **版本**：v1.0 · 数据驱动 + 上下文管理双轨版
> **最后更新**：2026-06-11
> **变更史**：从 v7.2 ContentFleet 双 Agent 体系重构而来，单 Agent + 三轮强制追问 + **飞书 100 篇历史数据反推** + **上下文管理 v1.0**。

## 一、定位

**新工作流 v0.1** 的最小可行骨架——专为新个人号起量期设计，对照 v7.2 的"重链路"做"轻链路"实验。

**核心思路**：
- 砍掉 Codex 教练组，单 Claude Code 全闭环
- 砍掉 9 Skills 矩阵，保留 3 个核心 + 沿用 v7.2 的合规/选题/产品库
- 强制三轮对话（撒网 → 深挖 → 换皮）替代单轮 prompt
- 三骨架填空替代 T1-T7 七步流水线

## 二、工作流（T1-T4）

```
T1 选题（10 分钟）       第一追问：5 个角度撒网 → 选 1
T1.5 骨架选型（5 分钟）   教程→问题链 │ 感悟→故事线 │ 观点→拆解面
T2 深度（15 分钟）       第二追问：核心观点推 3 层洞察
T3 写作（30 分钟）       第三追问：喂语调样本 + 骨架填空
T4 自检（10 分钟）       骨架完整性 + 三段一致性 + 评论钩子
─────────────────────
总计：约 70 分钟 / 篇
```

## 三、核心资产

| 类型 | 资产 |
| --- | --- |
| **核心 Prompt** | `prompts/` 下 5 个文件（README + 4 阶段） |
| **保留 v7.2 资产** | Skills/00-图文、01-选题、04-合规、10-活人、产品库 + Scripts/lint、validate、publish |
| **运营知识沉淀** | `运营方法论/` 5 篇 + `图文选题库数据运营/选题收敛方法论_v1.0_20260527.md` |
| **语调样本库** | `voice_samples/`（待用户提供 3-5 篇） |
| **验证跑记录** | `test_run/` |

## 四、绝对红线（Kill Switches）

1. **不许单轮 prompt 直接写正文**——必须走 T1 → T2 → T3 三轮
2. **不许跳骨架五步**——可以调顺序，不许删步
3. **不许用默认口吻**——T3 必须喂语调样本
4. **不许省略 T4 自检**——12 项骨架完整性必查
5. **不许堆配图**——每篇 1-2 张主图为限

## 五、T4 自检 4 档结论

| 结论 | 触发条件 | 下一步 |
| --- | --- | --- |
| ✅ 通过 | 12 项骨架完整性 + 三段一致性 + 评论钩子 + 合规 + 配图全通过 | 发布 |
| ⚠️ 小修后通过 | 仅 AI 味 WARNING 级 | 自动替换 → 通过 |
| 🔧 事实修订后复核 | T2 信源标注不符 / 反向配图校验 | 5-10 分钟快速复核 |
| ❌ 退回重练 | 骨架缺步 / 标题党 / 核心参数无法验证 | 回到 T1 / T2 重做 |

## 六、自动化工具链

```bash
# 自动化质检（v7.2 沿用）
python3 Scripts/lint_article.py outputs/你的文章.md --json

# 输出物结构校验
python3 Scripts/validate_outputs.py --verbose

# 头条自动发布（保留）
python3 Scripts/publish_toutiao.py --draft  # 草稿模式（默认）
python3 Scripts/publish_toutiao.py          # 发布模式（需 CEO 二次确认）
```

## 七、迭代日志

- **v0.1（2026-06-11）**：三追问 + 三骨架融合版初始化
  - 删除：Codex 教练组、Wechatsync 五平台、4 个开源集成 Skill、飞书排期、历史项目归档
  - 保留：核心 Skills（00/01/04/10/产品库）+ 自动化脚本（lint/validate/publish）
  - 新增：5 个核心 Prompt + 语调样本库 + 验证跑目录

## 八、与 v7.2 旧系统的关系

v7.2 是数据已验证但"不稳定"的旧链路，作为对照组保留所有规范文件与脚本。

**v0.1 验证通过前**：v7.2 仍是老号 A / B 的生产链路。
**v0.1 验证通过后**：根据新个人号 + 老号 A / B 数据对比，决定是否回灌。

详见 `prompts/README.md` 第五节。

## 九、数据反推核心洞察（100 篇历史复盘）

### A. 标题是流量的唯一开关

| 主因归因 | 占比 | 表现分层走向 |
|---|---|---|
| 标题弱 | 47% | 100% 沦为普通 |
| 标题强点击 | 19% | 100% 爆款或点击强 |
| 平台愿意推 | 21% | 全是爆款/推荐强 |

**结论**：标题决定生死。v0.1 的 T1 + T1.5 必须把"标题模板选型"做成强制步骤。

### B. 标题字数黄金区间：25-29 字

- < 20 字：CTR 2.71%（扑）
- 20-24 字：CTR 3.27%
- **25-29 字：CTR 4.90% ⭐️**
- 30-34 字：CTR 4.75%
- 35-44 字：CTR 5.0-5.5%（样本小）

### C. 反问悬念型标题是核武

4 篇样本中位展现 **33,303**，100% 进入爆款/点击强。

### D. 选题域优先级

| 品类 | 爆款率 | 建议 |
|---|---|---|
| 冰箱 | 57% | ⭐️ 首选 |
| 空调 | 45% | ⭐️ 首选 |
| 电视 | 50% | ⭐️ 主战场 |
| 显示器 | 50% | 样本小但都是爆款 |
| 洗衣机 | 11% | **避开** |

详见 `prompts/01-选题/选题_三追问.md` 完整 21 个爆款标题样本。

## 十、上下文管理 v1.0 工作模式（Claude Code 专属）

> **目的**：解决"单篇上下文就爆"问题，单篇占用的对话 token 从 ~30k 降到 < 5k。

### 工作模式铁律

**1. 规则在文件系统，对话只引用**

```
❌ 错误：把 prompts/01-选题/选题_三追问.md（7,718 字）粘贴进对话
✅ 正确：@/Users/aaron/.../prompts/01-选题/选题_三追问.md
```

Claude Code 的 `@文件路径` 语法：文件内容**只在需要时被加载到上下文**，不预先占用空间。

**2. 每 T 一对话**

5 个 T = 5 个独立会话。中间产物全落盘到 `test_run/`，对话不保留历史。

| T | 对话开头 |
| --- | --- |
| T1 | `读取 test_run/选题_第X篇.md（如不存在则新建），按 prompts/01-选题/选题_三追问.md 跑 T1` |
| T1.5 | `读取 test_run/选题_第X篇.md 的 T1 摘要，按 prompts/02-骨架选型/骨架选型速查表.md 选骨架+标题` |
| T2 | `读取 test_run/选题_第X篇.md 的 T1+T1.5 摘要，按 prompts/03-深度/深度_三追问.md 推 3 层` |
| T3 | `读取 test_run/选题_第X篇.md 全部记录 + assets/titles/[对应品类+模板].md，按 prompts/04-写作/写作_骨架填空模板.md 写正文` |
| T4 | `读取 test_run/选题_第X篇.md + test_run/成品_第X篇.md，按 prompts/05-自检/发布前自检清单.md 跑 12 项检查` |

**3. 进度标记 + YAML 接力**

每篇 `选题_*.md` 末尾追加：

```markdown
## 进度
- [x] T1（YYYY-MM-DD HH:MM）
- [x] T1.5
- [x] T2
- [ ] T3（进行中）
- [ ] T4
```

每完成一个 T，写 `test_run/_handoff/article_[ID]_T[阶段].yaml`。

**4. 跨会话恢复 5 分钟承诺**

任何断点：看 `test_run/选题_[最新].md` 进度 → 开新会话 → 用上面的"对话开头话术"。

详细见 `_protocols/cross_session_resume.md`。

### 上下文快满的 3 个信号

| 信号 | 应对 |
| --- | --- |
| AI 重复自己（"如前所述..."超过 3 次） | `/new` 开新会话 |
| AI 引用错的"前文" | **立刻停止**，开新会话 |
| AI 输出质量明显下降 | 开新会话 + 备份 |

### 单篇上下文预算

| 阶段 | 预计 token | 实际占用 |
| --- | --- | --- |
| T1 | < 3k | 待验证 |
| T1.5 | < 1k | 待验证 |
| T2 | < 4k | 待验证 |
| T3 | < 5k | 待验证 |
| T4 | < 2k | 待验证 |
| **单篇总计** | **< 15k** | **目标 < 5k** |

### 与 v7.2 RUN_STATE 对比

v7.2 RUN_STATE 把所有上下文塞进一个文件，结果文件本身成了"第二上下文炸弹"。

v1.0 用 3 招彻底解决：
1. 规则在文件系统（不在对话）
2. 每 T 独立会话（不累积）
3. 摘要固定 ≤300 字（不膨胀）